{% extends 'base_admin.html.twig' %}

{% block title 'Modifier Utilisateur' %}

{% block body %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Accueil</a></li>
        <li class="breadcrumb-item"><a href="{{ path('admin_dash_board_index') }}">Tableau de bord</a></li>
        <li class="breadcrumb-item">
            <a href="{{ path('admin_user_index') }}">Utilisateurs</a>
        </li>
        <li class="breadcrumb-item active">{{ block('title') }}</li>
    </ol>

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary text-center">Modifier l'utilisateur {{ user.nickname }}</h6>
        </div>
        {% include 'back_office/user/_form_user.html.twig' %}
    </div>
{% endblock %}

