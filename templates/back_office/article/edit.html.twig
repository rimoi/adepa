{% extends 'base_admin.html.twig' %}

{% block title %}modifier article {{ article.title }}{% endblock %}

{% block body %}
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary text-center">Modifier {{ article.title }}</h6>
        </div>
        {% include 'back_office/article/_form.html.twig' %}
    </div>
{% endblock %}
