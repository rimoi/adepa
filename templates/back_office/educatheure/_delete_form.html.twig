<form method="post" action="{{ path('admin_educatheure_delete', {'id': educatheure.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ educatheure.id) }}">
    <button type="submit" class="btn btn-danger">Supprimer</button>
</form>
